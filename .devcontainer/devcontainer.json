{
  "name": "cbackup Dev Container",
  "image": "ubuntu:22.04",
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {
      "installZsh": true,
      "username": "vscode"
    }
  },
  "postCreateCommand": "apt-get update && apt-get install -y build-essential cmake make git zlib1g-dev libgtest-dev valgrind cpplint && cd /usr/src/gtest && cmake . && make && cp lib/*.a /usr/lib/ || true",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools",
        "ms-vscode.cmake-tools",
        "twxs.cmake"
      ],
      "settings": {
        "C_Cpp.default.cppStandard": "c++17"
      }
    }
  },
  "remoteUser": "root"
}
